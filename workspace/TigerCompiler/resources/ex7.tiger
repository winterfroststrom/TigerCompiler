let
        type int_arr = array [100] of int;
        var a : int_arr := 0;
        var i : int;
in
        for i := 2 to 100 do
                a[i] := a[i] + a[i-1];
                printi(a[i]);
        enddo;
end
